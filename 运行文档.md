# AI名人模拟器 - 运行文档

## 快速开始

### 1. 环境要求
- **Go**: 1.24+
- **MySQL**: 8.0+
- **Redis**: 6.0+
- **阿里云百炼**: API密钥
- **阿里云OSS**: 存储配置
- **微信小程序**: AppID和AppSecret

### 2. 基础配置

#### 创建环境变量文件
```bash
# 在go目录下创建.env文件
cd go
vim .env
```

#### 必需配置项
```bash
# 数据库配置
MYSQL_DSN=user:password@tcp(localhost:3306)/ai?charset=utf8mb4&parseTime=True&loc=Local
REDIS_ADDR=localhost:6379

# 微信配置
WECHAT_APP_ID=your_wechat_app_id
WECHAT_APP_SECRET=your_wechat_app_secret

# 阿里云百炼配置
BAILIAN_API_KEY=your_bailian_api_key
BAILIAN_API_KEY_2=your_bailian_api_key_2
BAILIAN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# OSS配置
OSS_ACCESS_KEY_ID=your_oss_access_key_id
OSS_ACCESS_KEY_SECRET=your_oss_access_key_secret
OSS_BUCKET=your_oss_bucket
OSS_ENDPOINT=your_oss_endpoint

# 服务器配置
SERVER_PORT=9000
GIN_MODE=release
```

#### Redis配置
```bash
# 启动Redis服务
sudo systemctl start redis
sudo systemctl enable redis

# 验证Redis运行
redis-cli ping
# 应该返回 PONG

# 如果Redis设置了密码，在.env文件中添加：
# REDIS_PASSWORD=your_redis_password
```

### 3. 数据库初始化
```bash
# 执行数据库初始化脚本
mysql -u root -p < init_db.sql
```

### 4. 小程序配置
修改 `font/app.js` 中的API地址：
```javascript
globalData: {
  baseUrl: 'https://your-api-domain.com'
}
```

## Make命令使用

### 查看所有命令
```bash
make help
```

### 常用命令

#### 开发环境
```bash
# 安装依赖
make install

# 运行服务（前台）
make run

# 代码检查
make vet

# 更新依赖
make deps
```

#### 生产环境
```bash
# 构建二进制文件
make build

# 启动服务（后台）
make start

# 停止服务
make stop

# 重启服务
make restart

# 查看服务状态
make status
```

#### 维护命令
```bash
# 清理构建文件和日志
make clean
```

### 命令说明

| 命令 | 功能 | 使用场景 |
|------|------|----------|
| `make help` | 显示帮助信息 | 查看所有可用命令 |
| `make install` | 安装依赖包 | 首次运行或依赖更新 |
| `make build` | 构建应用程序 | 生产环境部署 |
| `make run` | 运行应用程序（前台） | 开发调试 |
| `make start` | 启动服务（后台） | 生产环境运行 |
| `make stop` | 停止服务 | 停止后台服务 |
| `make restart` | 重启服务 | 更新后重启 |
| `make status` | 查看服务状态 | 检查服务运行状态 |
| `make clean` | 清理文件 | 清理构建文件和日志 |
| `make vet` | 代码静态检查 | 代码质量检查 |
| `make deps` | 更新依赖 | 依赖包更新 |

### 开发流程

#### 1. 首次运行
```bash
# 1. 配置环境变量
vim .env

# 2. 安装依赖
make install

# 3. 初始化数据库
mysql -u root -p < init_db.sql

# 4. 运行服务
make run
```

#### 2. 日常开发
```bash
# 启动开发服务
make run

# 代码检查
make vet

# 更新依赖
make deps
```

#### 3. 生产部署
```bash
# 构建应用
make build

# 启动服务
make start

# 检查状态
make status
```

### 故障排除

#### 查看服务状态
```bash
make status
```

#### 重启服务
```bash
make restart
```

#### 查看日志
```bash
# 查看应用日志
tail -f logs/app.log

# 查看错误日志
tail -f logs/error.log
```

#### 检查服务状态
```bash
# 检查MySQL
sudo systemctl status mysql
mysql -u root -p -e "SHOW DATABASES;"

# 检查Redis
sudo systemctl status redis
redis-cli ping
# 如果设置了密码
# redis-cli -a your_password ping

# 检查Go服务
make status
```

#### 清理重新开始
```bash
# 清理所有文件
make clean

# 重新安装依赖
make install

# 重新运行
make run
```