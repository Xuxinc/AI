# AI名人模拟器 - 架构设计文档

## 系统架构概览

### 整体架构图
```
┌─────────────────────────────────────────────────────────────┐
│                    微信小程序前端                              │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ │
│  │  首页   │ │  聊天   │ │  会话   │ │  个人   │ │ 语音通话 │ │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘ │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ HTTPS/WebSocket
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Go后端服务层                              │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ │
│  │ 用户管理 │ │ 角色管理 │ │ 对话管理 │ │ 语音处理 │ │ 会话管理 │ │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘ │
└─────────────────────────────────────────────────────────────┘
                              │
                              │
┌─────────────────────────────────────────────────────────────┐
│                    数据存储层                                │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ │
│  │  MySQL  │ │  Redis  │ │   OSS   │ │ 百炼API │ │ 微信API │ │
│  │  数据库  │ │  缓存   │ │  存储   │ │  AI服务  │ │ 登录服务 │ │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 核心组件
- **前端层**: 微信小程序（用户界面）
- **应用层**: Go后端服务（业务逻辑）
- **数据层**: MySQL + Redis + OSS（数据存储）
- **AI层**: 阿里云百炼（AI能力）
- **外部服务**: 微信API（用户认证）

## 技术架构

### 后端技术栈
```
┌─────────────────────────────────────────────────────────────┐
│                    Go后端技术栈                              │
├─────────────────────────────────────────────────────────────┤
│ 语言: Go 1.24                                               │
│ Web框架: Gin                                                │
│ 数据库: MySQL 8.0 + GORM                                    │
│ 缓存: Redis                                                 │
│ 日志: Logrus                                                │
│ WebSocket: Gorilla WebSocket                               │
│ 云服务: 阿里云OSS、阿里云百炼                                │
└─────────────────────────────────────────────────────────────┘
```

### 前端技术栈
```
┌─────────────────────────────────────────────────────────────┐
│                  微信小程序技术栈                            │
├─────────────────────────────────────────────────────────────┤
│ 平台: 微信小程序                                             │
│ UI框架: 原生小程序组件                                       │
│ 音频处理: pcm-player                                        │
│ 网络请求: wx.request                                         │
│ WebSocket: wx.connectSocket                                 │
└─────────────────────────────────────────────────────────────┘
```

## 模块设计

### 1. 后端模块架构

#### 1.1 配置管理模块 (config/)
**职责**: 系统配置管理和环境变量处理
- **config.go**: 配置结构定义和加载
- **功能**: 
  - 环境变量解析
  - 配置验证
  - 默认值设置

#### 1.2 数据库模块 (database/)
**职责**: 数据库连接和初始化
- **database.go**: 数据库连接管理
- **功能**:
  - MySQL连接池
  - Redis连接管理
  - 连接健康检查

#### 1.3 数据模型模块 (models/)
**职责**: 数据模型定义和ORM映射
- **models.go**: 数据模型结构
- **实体**:
  - User（用户）
  - Character（角色）
  - Dialog（会话）
  - Message（消息）

#### 1.4 路由模块 (routes/)
**职责**: API路由配置和请求分发
- **routes.go**: 路由定义
- **功能**:
  - RESTful API路由
  - WebSocket路由
  - 中间件集成

#### 1.5 处理器模块 (handlers/)
**职责**: HTTP请求处理和响应
- **user.go**: 用户相关接口
- **character.go**: 角色相关接口
- **chat.go**: 聊天相关接口
- **voice.go**: 语音相关接口
- **upload.go**: 文件上传接口

#### 1.6 业务服务模块 (services/)
**职责**: 核心业务逻辑实现
- **ai_stream.go**: AI对话服务
- **character_generator.go**: 角色生成服务
- **voice_synthesis.go**: 语音合成服务
- **voice_recognition.go**: 语音识别服务
- **voice_clone.go**: 音色克隆服务
- **oss_upload.go**: 文件上传服务
- **wechat.go**: 微信服务
- **user.go**: 用户服务

#### 1.7 WebSocket模块 (websocket/)
**职责**: 实时通信处理
- **voice_chat.go**: 语音聊天WebSocket
- **功能**:
  - 实时语音识别
  - 实时语音合成
  - 双向音频流传输

#### 1.8 中间件模块 (middleware/)
**职责**: 请求中间件处理
- **auth.go**: 身份认证中间件
- **功能**:
  - Token验证
  - 用户身份识别
  - 权限控制

#### 1.9 工具模块 (utils/)
**职责**: 通用工具函数
- **cache.go**: 缓存工具
- **redis.go**: Redis操作
- **snowflake.go**: ID生成
- **logrus_logger.go**: 日志工具
- **logrus_middleware.go**: 日志中间件

### 2. 前端模块架构

#### 2.1 页面模块 (pages/)
**职责**: 小程序页面实现
- **index/**: 首页模块
  - 角色展示
  - 角色选择
  - 搜索功能
- **chat/**: 聊天模块
  - 文字对话
  - 图片上传
  - 流式显示
- **conversations/**: 会话模块
  - 会话列表
  - 会话管理
  - 置顶功能
- **profile/**: 个人中心模块
  - 用户信息
  - 设置管理
- **voice-call/**: 语音通话模块
  - 实时语音
  - 音频处理
- **character-detail/**: 角色详情模块
  - 角色信息
  - 角色设置

#### 2.2 资源模块
- **images/**: 图片资源
- **i18n/**: 国际化资源
- **app.js**: 应用入口
- **app.json**: 应用配置

## 数据架构

### 数据库设计

#### 核心表结构
```sql
-- 用户表
users (id, openid, nickname, avatar, created_at, updated_at)

-- 角色表
characters (id, name, description, prompt, voice_model, avatar_url, is_created_by_user, uid, created_at, updated_at)

-- 会话表
dialogs (id, user_id, character_id, is_top, created_at, updated_at)

-- 消息表
messages (id, dialog_id, content, is_voice, voice_url, picture_url, is_deleted, role, time)
```

#### 数据关系
```
users (1) ──→ (N) dialogs (N) ──→ (1) characters
  │                    │
  │                    └──→ (N) messages
  │
  └──→ (N) characters (自定义角色)
```

### 缓存架构
- **Redis缓存策略**:
  - 用户信息缓存
  - 角色信息缓存
  - 会话列表缓存
  - API响应缓存

### 存储架构
- **MySQL**: 结构化数据存储
- **Redis**: 缓存和会话存储
- **OSS**: 文件存储（图片、音频）

## 接口设计

### RESTful API设计

#### 用户相关接口
```
POST /api/user/login          # 微信登录
GET  /api/user/info           # 获取用户信息
POST /api/user/wechat-info    # 上传微信信息
PUT  /api/user/info           # 更新用户信息
POST /api/user/upload-avatar  # 上传头像
POST /api/user/logout         # 退出登录
```

#### 角色相关接口
```
GET  /api/characters/public           # 获取公开角色
GET  /api/characters                  # 获取所有角色
GET  /api/characters/search           # 搜索角色
POST /api/characters/generate-celebrity # 生成名人角色
POST /api/characters/generate-custom    # 生成自定义角色
POST /api/characters/upload-avatar     # 上传角色头像
GET  /api/characters/:id              # 获取角色详情
PUT  /api/characters/:id              # 更新角色信息
DELETE /api/characters/:id             # 删除角色
```

#### 聊天相关接口
```
GET  /api/chat/dialog         # 获取对话历史
POST /api/chat/message        # 发送消息
POST /api/chat/upload-images # 上传图片
POST /api/chat/delete-message # 删除消息
```

#### 会话相关接口
```
GET  /api/conversations       # 获取会话列表
POST /api/conversations/pin   # 置顶会话
DELETE /api/conversations/:characterId # 删除会话
```

#### 语音相关接口
```
POST /api/upload/voice        # 上传音频文件
POST /api/voice/create       # 创建音色模型
```

#### WebSocket接口
```
GET /ws/voice-chat           # 语音通话WebSocket
```
